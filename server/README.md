# TODO App Backend (Express + MySQL)

This is the backend API server for the TODO application. It uses Node.js, Express, and MySQL.

## Requirements

- Node.js 18+
- MySQL 8+ (or compatible)
- npm or yarn

## Environment Variables

Copy `.env.example` to `.env` and set your values:
